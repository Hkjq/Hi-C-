#include <iostream>
using namespace std;

//数组倒叙

int main() {
	
	int arr[] = { 1,2,3,4,5 };
	int max = arr[0];
	int num = sizeof(arr) / sizeof(arr[0]); // 数组
	cout << num << endl;
	for (int i = num - 1; i >= 0; i--)
	{
		cout << arr[i] << endl;
	}



	system("pause");
	return 0;
}
