class Solution {
public:
    void sortIntegers(vector<int> &A) {
        // write your code here
         int i,j;
        for(i=0;i<A.size()-1;i++)
            for(j=0;j<A.size()-i-1;j++)
                if(A[j]>A[j+1])
                    swap(A[j],A[j+1]);
    }    
};
